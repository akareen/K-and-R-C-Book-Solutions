#include <stdio.h>
#define MAX_LINE_LENGTH 100
#define MAX_NUM_LINES 100

int main() {
    char inputData[MAX_LINE_LENGTH][MAX_NUM_LINES];
    
}

int * countWords() {}

int * frequencyCalculator(int maxNum, int array[], int arraySize) {
    int frequency_array[maxNum];
    for (int i = 0; i < arraySize; i++) {
        frequency_array[i]++;
    }
    return frequency_array;
}